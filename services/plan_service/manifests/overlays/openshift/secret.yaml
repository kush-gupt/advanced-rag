# TEMPLATE: Create actual secret before deploying:
#   oc create secret generic plan-service-secrets \
#     --from-literal=OPENAI_API_KEY="your-key" -n <namespace>
apiVersion: v1
kind: Secret
metadata:
  name: plan-service-secrets
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_WITH_ACTUAL_API_KEY"
